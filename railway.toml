# Builds are immutable. Committed migrations run once before the web replica
# starts, so a failed migration blocks the release without replica races.
[build]
buildCommand = "keystone build --no-ui && next build"

[deploy]
preDeployCommand = "npm run release:migrate"
startCommand = "next start"
healthcheckPath = "/api/health"
healthcheckTimeout = 300
